import clsx from 'clsx';
import type { Metadata } from 'next';
import dynamic from 'next/dynamic';
import { Inter } from 'next/font/google';

import './globals.css';
import { HeaderSkeleton } from '@/UI/seletons';

const Header = dynamic(() => import('@/components/Header'), {
  loading: () => <HeaderSkeleton />,
});

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

// simple text editor (try to find in google or first idea is to select a word and then for example make it bold and after that this word in db will change from 'test' to '<b>test</b>' and when the text must be presented on the site cycle will find that <b> tag or another html tag);

// in edit mode post must contain image editor
// preview post update

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang='en'>
      <body className={clsx(inter.className, 'bg-black/30')}>
        <Header />
        <div className={clsx('w-full pt-[72px] px-[3%]', 'sm:px-[5%]', 'lg:px-[15%]')}>
          {children}
        </div>
      </body>
    </html>
  );
}
